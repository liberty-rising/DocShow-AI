apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  template:
    spec:
      containers:
      - name: backend
        image: registry.digitalocean.com/docshow-ai/backend:latest
        envFrom:
        - configMapRef:
            name: backend-config
