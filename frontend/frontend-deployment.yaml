apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  template:
    spec:
      containers:
      - name: frontend
        image: registry.digitalocean.com/docshow-ai/frontend:latest
        envFrom:
        - configMapRef:
            name: frontend-config
